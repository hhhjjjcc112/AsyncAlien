[package]
name = "sys_vdso_time_compare"
version = "0.1.0"
edition = "2024"

[dependencies]
Mstd = { path = "../../../userlib" }
pconst = { path = "../../../../utils/pconst" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"