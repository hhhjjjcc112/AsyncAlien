# x86_64 域加载与关键域测试报告（task_list 第6条）

## 1. 执行命令与目标

- 执行命令：`timeout 240s env ARCH=x86_64 DOMAIN_TEST=y make run |& tee run.txt`
- 目标：在 x86_64 下验证 `syscall/task/apic/uart/block/net_stack` 六类关键域是否可用，并为每类测试输出中间步骤日志。
- 说明：本轮聚焦“最小可运行”闭环，不追求覆盖所有可选域。

## 2. 当前已加载域清单（来自本轮 run.txt）

### 2.1 已注册域（register）

1. apic
2. buf_input
3. buf_uart
4. cache_blk
5. devfs
6. domainfs
7. fatfs
8. fifo_scheduler
9. logger
10. net_stack
11. null
12. pipefs
13. procfs
14. ramfs
15. random
16. shadow_blk
17. syscall
18. sysfs
19. task
20. uart16550
21. vfs
22. virtio_blk

### 2.2 已附着别名（attach）

- apic -> apic
- block -> block-1
- buf_uart -> buf_uart
- cache_blk -> cache_blk-1
- devfs -> devfs
- domainfs -> domainfs
- fatfs -> fatfs-1
- logger -> logger
- net_stack -> net_stack
- nic -> nic-1
- null -> null
- pipefs -> pipefs
- procfs -> procfs
- ramfs -> ramfs-1/2/3/4
- random -> random
- scheduler -> scheduler
- shadow_blk -> shadow_blk-1
- syscall -> syscall
- sysfs -> sysfs
- task -> task
- uart -> uart
- vfs -> vfs
- virtio_block -> virtio_block-1
- virtio_net -> virtio_net-1（日志显示为 empty domain）

## 3. 哪些域值得优先添加测试

### 3.1 P0（本轮实测）

1. syscall
   - 原因：x86_64 Linux ABI 归一化链路长、历史问题多，回归价值最高。
2. task
   - 原因：直接决定 `/tests/init`、`/bin/sh` 拉起与调度稳定性。
3. apic
   - 原因：x86_64 时钟中断与外部中断分发核心依赖。
4. buf_uart
   - 原因：最小交互链路关键设备，直接影响 shell 可见性。
5. block
   - 原因：`fatfs` 挂载与 `/tests` 程序加载依赖块设备可用性。
6. net_stack
   - 原因：网络 syscall 路径依赖 net 栈域；可通过 socket_pair 做不依赖外部网络的稳定自测。

### 3.2 P1（建议后续补测）

- virtio_net 真链路收发：当前环境里 `virtio_net` 可能为 empty domain，需在完整网卡驱动产物可用时补强。
- cache_blk / shadow_blk：当前由主链路间接覆盖，可在后续加读写一致性测试。

### 3.3 P2（当前不优先）

- buf_input / virtio_input / virtio_gpu：本轮环境缺输入与图形设备，不适合做稳定自动化断言。

## 4. 测试用例设计与实现

## 4.1 用例设计（最小可运行）

1. `domain_syscall_test`
   - 严格检查：`domain_id` 非 0；两次 `SYSCALL_DOMAIN_TEST(2003)` 返回值均为 0。
   - 中间输出：resolve/probe 阶段日志（domain_id、每次 call 返回值）。
2. `domain_task_test`
   - 严格检查：`domain_id` 一致且非 0；`page_table_token_with_trap_frame_virt_addr`、`trap_frame_phy_addr` 在未初始化阶段允许 `EINVAL`（记录为跳过），其余错误判失败。
   - 中间输出：task 初始化前状态探测日志。
3. `domain_apic_test`
   - 严格检查：`domain_id` 非 0；`irq_info` 返回缓冲区非空。
   - 中间输出：`irq_info_len` 与预览字节。
4. `domain_uart_test`
   - 严格检查：`have_space_to_put=true`，`putc/put_bytes` 成功且写入长度非 0。
   - 中间输出：空间状态、写入长度、是否有输入数据；仅在有输入数据时尝试 `getc`。
5. `domain_block_test`
   - 严格检查：`domain_id` 非 0；容量/读块/flush 至少一个探测成功；容量若返回则必须大于 0。
   - 中间输出：容量值、首块读取长度与 checksum、flush 结果。
6. `domain_net_test`
   - 严格检查：`net_stack` 域存在且 `domain_id` 非 0；`socket_pair -> poll(EPOLLOUT) -> write -> poll(EPOLLIN) -> read -> remove_socket` 全链路通过。
   - 严格检查（nic 分支）：若 `nic.domain_id != u64::MAX`（非 empty domain），自动启用 `can_transmit`/`can_receive` 探测与 `transmit/receive` 强断言；若 `nic.domain_id == u64::MAX`，记录跳过。
   - 中间输出：socket id、poll 结果、读写长度、read 后 poll 状态、nic 探测信息。

## 4.2 代码实现位置

- `kernel/src/domain/test.rs`
  - `test_syscall`
  - `test_task`
  - `test_apic`
  - `test_uart`
  - `test_block`
   - `test_net`
  - `run`
- `kernel/src/domain/mod.rs`
  - x86_64 下在 `init_device()` 之后调用 `test::run()`。
- `kernel/Cargo.toml`
   - `domain_test` 作为总开关，包含六个子测试 feature（新增 `domain_net_test`）。
- `Makefile`
   - `DOMAIN_TEST` 与 `DOMAIN_*_TEST`（新增 `DOMAIN_NET_TEST`）映射到 kernel features。

## 5. 本轮执行结果

从 `run.txt` 抽取到以下关键标记：

- `[domain_test] begin`
- `[domain_syscall_test] pass: syscall`
- `[domain_task_test] pass: task`
- `[domain_apic_test] pass: apic`
- `[domain_uart_test] pass: uart`
- `[domain_block_test] pass: block`
- `[domain_net_test] pass: net`
- `[domain_test] done`

补充：本轮 `nic-1` 的 `domain_id` 为 `u64::MAX`，判定为 empty-domain proxy，因此按设计跳过严格 NIC I/O 分支，不影响 `net_stack` 主链路结论。

结论：本轮 P0 六项关键域测试通过（其中 nic 硬件探测为受限跳过），可支撑 x86_64 最小可运行目标。

## 6. 本轮有意简化与后续完善建议

### 6.1 有意简化（当前不实现）

- 对 empty-domain NIC（`domain_id == u64::MAX`）跳过严格 I/O 断言，仅保留明确 skip 日志，避免最小链路环境的假失败。
- block 用例仍未加入写入破坏性验证（只做容量/读块/flush），避免影响镜像状态与引导结果。

### 6.2 建议后续完善

- 在具备真实 NIC 域的环境下，补一轮运行日志采样，确认已命中 `nic transmit ok` 与（可接收时）`nic receive len` 分支。
- 为 block 增加小文件读写一致性测试（通过 vfs 路径落地，避免直接绑底层驱动细节）。
- 为 task 增加轻量 fork/exec/wait 组合断言，覆盖更多调度边界。
