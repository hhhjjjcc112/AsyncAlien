## task 26 排查列表

- [x] 已在 `cpu_loop()` 的新任务切换点补齐 x86_64 `TSS.rsp0` 刷新，并用 SMP=2 直接 QEMU 复验通过。
- [x] 扩大 x86_64 临时 tracing 范围，覆盖 `syscall_all` 的 parent/child tid，先把 cpu1 首跑的证据链补全。
- [ ] 复跑 `ARCH=x86_64 SMP=2 make record_run`，确认 tid=3 是否能看到 `child_first_run`、`prepare_user_return`、`trap_return` 和首个 syscall。
- [ ] 对照轮转从 0 开始/从 1 开始两次记录，判断差异是出在首次进入用户态，还是出在第一次 syscall/waitpid。
- [x] 将 x86_64 Local APIC 改成 per-CPU 槽位，先消除 SMP 下共享 `&mut LocalApic` 的可变别名风险。
- [x] 将 x86_64 新任务暂时固定到 cpu0，先绕开 cpu1 的首个用户态返回卡点，恢复 shell 再单独修 AP 路径。
- [ ] 若 tid=3 只看到 `trap_return` 但没有后续 syscall，再检查 secondary CPU 的 trap/GDT/TSS 初始化顺序。
- [ ] 若 tid=3 能进入 syscall 但卡在 waitpid / wake_up_wait_task，再追 wait/exit 跨核唤醒链路。
- [ ] 证伪的排查项要及时删除对应临时日志，避免后续记录被噪声干扰。
